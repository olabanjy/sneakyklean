// Main homepage bootstrap.
// Keep this file as the shared entry point for page-wide behaviors.
(function () {
  function initMain() {
    document.body.classList.add("js-enabled");
  }

  document.addEventListener("DOMContentLoaded", initMain);
})();
